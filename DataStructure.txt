//file functions

#include<stdio.h>
void main()
{
   FILE *fp;
   char ch;
   clrscr();
   //writing data into file
   fp =fopen("test.txt","w");
   while((ch=getchar())!=EOF)
   {
     fputc(ch,fp);
     printf("%c",ch);
   }
   fclose(fp);
   //reading data into file
   fp =fopen("test.txt","r");
   while((ch=getchar())!=EOF)
   {
     fputc(ch,fp);
     printf("%c",ch);
   }
   fclose(fp);
}
